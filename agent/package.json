{
  "name": "forge-agent",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Forge agent loop. Runs inside GitHub Actions on a repository_dispatch trigger. Drives puter.js inside a real headless Chromium page for reliable tool-calling.",
  "scripts": {
    "start": "node run.js"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  }
}
